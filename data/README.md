# Training data

`shakespeare.txt` is the "tiny Shakespeare" corpus: roughly 1.1 MB of
concatenated Shakespeare plays, widely used as a benchmark for small
language models (popularised by char-rnn and nanoGPT). The text itself is
public domain. It is committed to the repo so that training runs are fully
offline and reproducible.
